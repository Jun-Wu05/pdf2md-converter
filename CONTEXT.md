# 领域术语表 — PDF→Markdown 完整转换

> 本文件仅是词汇表,记录稳定业务术语的精确含义。不含实现细节、技术选型或视觉规格。
> 实现决策见 `docs/adr/`,视觉规格见 `docs/design/DESIGN.md`(本项目 headless,无设计文档)。

---

## 完整转换 (Complete Conversion)

把一份上传的 PDF 转成一份 Markdown,成功标准是**语义/结构保真优先**:

- **文本不丢**:正文、步骤、说明性文字必须完整进入 Markdown,不得因布局检测误判而遗漏。
- **结构尽量还原**:标题层级、有序/无序列表、代码块、字段表尽量还原为对应 Markdown 结构,而非拍平成纯文本。
- **允许降级**:对 pdf-inspector 当前解析不了的复杂表(多行合一、跨页、无矩形框的语义表),允许降级为**近似表示**(如保留全部单元格文本、用最接近的表格或定义列表承载),而不是转换失败或产出乱序内容。
- **不追求**:版面像素级还原、字体颜色、页眉页脚装饰;这些不属于「完整」。

**对照边界**:速度不是第一目标(当前 ~140ms 已足够),表格结构精度才是核心痛点。

---

## 表格还原 (Table Restoration)

把 PDF 中的表格转成 Markdown 的过程,分两档验收:

- **结构优先**:能正确成表(列对齐、表头正确、单元格不错位)时,输出标准 Markdown 表格。
- **文本兜底**:当 pdf-inspector 无法精确还原结构(多行合一、跨页、无矩形框的中文语义表)时,**不丢字、不串行**即算成功——把全部单元格文本按行顺序保留,用近似表(`| 单元格 | 单元格 |`)或退化为「字段: 值」列表/原序文本承载。
- **失败定义**:丢失单元格文本、或单元格错位串到错误行/列,才算失败。

**依据**:本项目处理的多为无线框中文文档表,rect / line 策略常失效;文本兜底保证内容完整,是 ROI 最高的验收底线。

---

## 字段表 (Field Table)

安全厂商 syslog / 告警手册的主体结构,以「字段名 + 释义 + 数据类型 + 示例 + 说明」为典型列的多列对照表。证据见附件:`EDR+syslog`、`邮件网关 SYSLOG`、`外发说明文档`、`网神SecIPS3600`、`微步ONESIG`、R4.15.1 §5.3。

**特征**:无线框或仅顶/底有横线、列宽不均(说明列很长,字段名列很窄)、单元格常换行、跨页时表头重复、多行表头(如 SecIPS 表头分两行)。

**验收两档**(与「表格还原」一致):
- **结构优先**:列头正确、每行字段对齐、单元格不串列 → 标准 Markdown 表。
- **文本兜底**:列数错乱或多行表头无法对齐时,**字段名 ↔ 释义/说明成对不丢、不串行**即算成功,可退化为定义列表或近似表。
- **失败**:字段名错配到别的字段说明、或整行丢失。

**为什么单列**:这类表是本批数据的高频主体,也是 pdf-inspector 当前最易出错的表型(rect/line 无框可依、heuristic 易误判列数)。单列术语是为了让「字段名↔说明成对」这条验收底线有精确适用对象。

---

## 跨页表格 (Cross-page Table)

同一张表被分页打断。**要求**:续页不重复表头(或重复时识别为续表而非新表)、行连续、不因分页把一行拆成两半。pdf-inspector 已有 continuation row merging,但字段表跨页时仍出现重复表头拆段(如 R4.15.1 §5.3 拆成两段、各自重发表头),属回归重点。

---

## 降级表示 (Degraded Representation)

当行列对应无法保住时采用的兜底形态,按优先级:

- **定义列表**:`字段名 — 释义:类型(示例)`,逐行保留全部单元格文本,字段名与说明成对。
- **逐行平铺**:每行单元格文本顺序罗列,不丢字。
- **禁止**:为凑 Markdown 表而丢弃单元格、错位合并、产出乱序内容。

降级是**主动选择**,不是失败;产出必须可读且文本零丢失。

---

## 解码乱码 (Decoding Garble)

CJK 文字因 ToUnicode CMap 解码错误产生的乱码(字符在但内容错,如 R4.15.1 §5.5 的 `入网网日目入日非高`)。**与「丢字」是两类问题**:丢字是完整性失败;乱码是质量缺陷但字符在场。

**领域判定**:乱码不阻塞整份「完整转换」成功(字符未丢失),但**不得静默流出**——检测到时须标注(如 `[疑似解码错误]`),原文照留,供下游人工或后续 OCR 针对性修复。修复解码根因属 Rust 内核 `tounicode.rs` 范畴,不在本项目层硬性要求。
